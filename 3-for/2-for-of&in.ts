// for...of 
let arr = ['a', 'b', 'c'];
for (let element of arr) {
    console.log(element);
}


console.log(`============================================`);
// for...in
for (let index in arr) {
    console.log(index);
}